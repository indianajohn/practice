#pragma once
#include <stddef.h>

namespace prac {
template <typename T> class vector {
public:
  vector(const size_t &num_elem = 0, const T &default_value = T()) {
    // TODO
  }
  void push_back(const T &new_elem) {
    // TODO
  }
};
}; // namespace prac
